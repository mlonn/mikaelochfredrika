import styled from 'styled-components';

export default styled.textarea`
  resize: none;
  border: 2px solid #707070;
  border-radius: 8px;
  padding: 8px 12px;
  font-size: 20px;
  font-family: inherit;
  color: inherit;
  font-style: italic;
`;
